# homebrew-tap

## Install with brew

- Add our tap:

```bash
brew tap saladict-app/homebrew-tap
```

- Install pot:

```bash
brew install --cask saladict
```

- Upgrade pot

```bash
brew upgrade --cask saladict
```
